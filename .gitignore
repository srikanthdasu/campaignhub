# root-level ignores; backend/ and frontend/ carry their own .gitignore too
.env
.env.*
!.env.example
.DS_Store
CamainHub AI Rebuild .txt

# raw brand source assets — optimized/cropped copies live in frontend/public/brand
/images/
